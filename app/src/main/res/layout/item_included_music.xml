<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="157dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="16dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <ImageView
        android:id="@+id/iv_included_music"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/img_included_music_1"
        />
    <TextView
        android:id="@+id/tv_included_title"
        android:theme="@style/TextAppearance.Spotify.Body.Medium_en"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="The Carter IV (Complete Edition)"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/iv_included_music"
        app:layout_constraintStart_toStartOf="parent"
        />
    <TextView
        android:id="@+id/tv_included_release"
        android:theme="@style/TextAppearance.Spotify.Body.Medium_en"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="2011"
        android:textColor="@color/gray30"
        app:layout_constraintTop_toBottomOf="@id/tv_included_title"
        app:layout_constraintStart_toStartOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>